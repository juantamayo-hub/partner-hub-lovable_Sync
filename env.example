# Copia este archivo a .env y rellena los valores.
# En Windows: copy env.example .env

# --- Saltar login (solo desarrollo) ---
# Vite/Lovable: usa VITE_SKIP_AUTH=true (en Vite solo se exponen variables VITE_*).
# Next: usa NEXT_PUBLIC_SKIP_AUTH=true.
# VITE_SKIP_AUTH=true
# NEXT_PUBLIC_SKIP_AUTH=true

# --- URL del backend (solo para build Vite / Lovable) ---
# Cuando el front (Vite) y el API no están en el mismo servidor, pon aquí la URL base del backend.
# Ejemplo: tu app Next desplegada en Vercel.
# Dejar vacío o no definir si usas Next (mismo origen).
VITE_API_BASE_URL=https://tu-app.vercel.app

# --- Supabase (si usas .env en lugar de hardcodear en código) ---
# NEXT_PUBLIC_SUPABASE_URL=
# NEXT_PUBLIC_SUPABASE_ANON_KEY=

# --- Google Sheets (backend Next / API) ---
# GOOGLE_SHEETS_ID=
# GOOGLE_LEADS_SHEET_TAB=Bayteca_leads_2026
# GOOGLE_DUPES_SHEET_TAB=B2B Copy
# GOOGLE_USERS_SHEET_TAB=Users
